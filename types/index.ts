export type NavLink = {
  href: string;
  label: string;
};

export type Case = {
  client: string;
  segment: string;
  challenge: string;
  approach: string;
  solution: string;
  outcome: string;
};

export type Service = {
  id: string;
  title: string;
  description: string;
  icon: string;
};
